#This is a simple text editor using Tkinter, one of the standard python GUI package

import tkinter as tk
#creating Tk root widget
root = tk.Tk(className = 'mero-text, a plain text editor')

root.mainloop()
